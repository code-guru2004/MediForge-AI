import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "MediForge AI",
  description: "Empowering healthcare with intelligent diagnostics and predictive analysis. Our Machine Learning platform delivers accurate insights from medical data, helping doctors make faster and better clinical decisions. We combine advanced AI with secure health technologies to improve patient outcomes, reduce risks, and bring precision medicine to everyone.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
